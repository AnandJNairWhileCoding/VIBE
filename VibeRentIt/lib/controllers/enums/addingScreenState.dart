enum AddingScreenState{
residenceName,
residenceType,
bedRoom,
washRoom,
carpetArea,
parking,
location,
images, 
cost
}



// residenceName(context)
                  // residenceType()
                  // bedAndWashRooms(context)
                  // parking()
                  // location(context)
                  // locationWidgetDecider(context)
                  // imageButton()